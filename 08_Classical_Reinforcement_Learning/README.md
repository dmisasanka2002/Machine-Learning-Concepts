Per the scope discussion: classical RL (tabular, MDP-based) belongs in this
ML repo; deep RL (DQN, policy-gradient neural nets, actor-critic with
function approximation) belongs in your DL repo. This section replaces the
thin `Concepts/04 - reinforcement_learning.ipynb`.
Prerequisites: Probability primer, ML Theory (risk/ERM framing carries over
conceptually — RL replaces "loss on a fixed dataset" with "expected
cumulative reward under a policy").